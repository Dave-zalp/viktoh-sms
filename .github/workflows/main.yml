name: 🚀 Deploy Viktohs SMS (Frontend + Backend) to Hostinger via SSH

on:
  push:
    branches:
      - main

jobs:
  deploy-backend:
    name: Deploy Backend to Host
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, bcmath, curl, xml, ctype, json, tokenizer
          tools: composer

      - name: Install PHP dependencies
        working-directory: viktoh_sms_back
        run: composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev

      - name: Remove unnecessary files
        working-directory: viktoh_sms_back
        run: |
          rm -rf .git .github tests node_modules
          rm -rf storage/framework/cache/* storage/framework/sessions/* storage/framework/views/* storage/logs/*
          rm -f .env .env.example

      # Uploads the CONTENTS of viktoh_sms_back/ directly into the deploy path
      # (i.e. deploy_path/artisan, not deploy_path/viktoh_sms_back/artisan)
      - name: Deploy backend files via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          username: ${{ secrets.VIKTOHSMS_SSH_USER }}
          server: ${{ secrets.VIKTOHSMS_SSH_HOST }}
          port: ${{ secrets.VIKTOHSMS_SSH_PORT }}
          ssh_private_key: ${{ secrets.VIKTOHSMS_SSH_KEY }}
          local_path: './viktoh_sms_back/'
          remote_path: ${{ secrets.VIKTOHSMS_BACKEND_DEPLOY_PATH }}
          sftp_only: true

      - name: Run backend deployment commands
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VIKTOHSMS_SSH_HOST }}
          username: ${{ secrets.VIKTOHSMS_SSH_USER }}
          key: ${{ secrets.VIKTOHSMS_SSH_KEY }}
          port: ${{ secrets.VIKTOHSMS_SSH_PORT }}
          script: |
            set -e

            echo "📍 Current location:"
            pwd

            echo "📂 Changing to deployment directory..."
            cd ${{ secrets.VIKTOHSMS_BACKEND_DEPLOY_PATH }}

            echo "📍 Now at:"
            pwd

            echo "📄 Files in directory:"
            ls -la

            echo "🔍 Looking for artisan:"
            ls -l artisan || echo "❌ artisan file not found!"

            echo "🔍 Checking for views directory:"
            ls -la resources/views || echo "❌ views directory not found!"

            echo "📁 Creating necessary directories..."
            mkdir -p storage/framework/{sessions,views,cache}
            mkdir -p storage/logs
            mkdir -p bootstrap/cache

            echo "🔄 Running migrations..."
            php artisan migrate

            # Import service names to database
            # php artisan sms:import-services

            # echo "Composer autoload dump"
            # composer dump-autoload

            # echo "Seeding database"
            # php artisan db:seed

            echo "🗑️ Clearing old cache..."
            php artisan optimize:clear

            echo "⚡ Optimizing Laravel..."
            php artisan config:clear

            # # Only cache routes if you have route closures converted to controller methods
            # # If you have closures in routes/web.php or routes/api.php, skip this
            # if php artisan route:cache 2>/dev/null; then
            #   echo "✅ Routes cached successfully"
            # else
            #   echo "⚠️ Route caching skipped (you may have route closures)"
            # fi

            echo "🔒 Setting permissions..."
            chmod -R 775 storage bootstrap/cache

            # If you're on shared hosting, you might need:
            # find storage bootstrap/cache -type f -exec chmod 664 {} \;
            # find storage bootstrap/cache -type d -exec chmod 775 {} \;

            echo "✅ Backend deployment complete!"

  deploy-frontend:
    name: Deploy Frontend to Host
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install frontend dependencies
        working-directory: Viktohsms_front
        run: pnpm install --frozen-lockfile=false

      - name: Build frontend
        working-directory: Viktohsms_front
        run: pnpm run build

      # Uploads the CONTENTS of the built dist/ folder directly into the deploy path
      # (i.e. deploy_path/index.html, not deploy_path/Viktohsms_front/index.html)
      - name: Deploy frontend files via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          username: ${{ secrets.VIKTOHSMS_SSH_USER }}
          server: ${{ secrets.VIKTOHSMS_SSH_HOST }}
          port: ${{ secrets.VIKTOHSMS_SSH_PORT }}
          ssh_private_key: ${{ secrets.VIKTOHSMS_SSH_KEY }}
          local_path: './Viktohsms_front/dist/'
          remote_path: ${{ secrets.VIKTOHSMS_FRONTEND_DEPLOY_PATH }}
          sftp_only: true

      - name: Deployment complete
        run: echo "✅ Frontend deployment complete!"
